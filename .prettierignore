dist/
build/
worker/public/
.next/
out/
.wrangler/
pnpm-lock.yaml
worker/worker-configuration.d.ts

# Prettier drops comments when reformatting .jsonc, and the wrangler config is
# where the origin-isolation and binding rationale has to live.
*.jsonc

# Served verbatim to crawlers and to Cloudflare's asset router, which both care
# about exact line shape.
website/public/

# The tutorial deck is an artifact. It is stored and served byte-for-byte down
# the same path as anything a stranger uploads, and the tour's seeded notes
# anchor into it by quoted text and nth-of-type path, so reformatting it can
# move or orphan them. We do not rewrite artifacts, including our own.
worker/tutorial/
